name: Reuse Workflow

on:
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo ${{ inputs.python-version }}
